export class Device {
  serialNumber: string;
  mostRecentFirmwareVersion: string;

  // FKs deviceAccount
  deviceAccountId: string;
  // reports?: Report[];
  // deviceAccount?: DeviceAccount;
  // alerts?: Alert[];
}
